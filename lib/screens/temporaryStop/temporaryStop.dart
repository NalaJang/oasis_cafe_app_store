import 'package:flutter/material.dart';

class TemporaryStop extends StatelessWidget {
  const TemporaryStop({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
